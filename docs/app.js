let data=[],assets={};const missing="לא צוין באתר שמרלינג";
const $=s=>document.querySelector(s),arr=v=>!v?[]:Array.isArray(v)?v:[v];
const dimParts=v=>{const p=String(v||"").trim().split(/[xX×]/).map(x=>x.trim());return p.length===3&&p.every(Boolean)?p:null};
const dim=v=>{const p=dimParts(v);return p?`אורך ${p[0]} מטר · רוחב ${p[1]} מטר · גובה ${p[2]} מטר`:missing};
function dimensions(v){const p=dimParts(v);return p?`<div class="dim" aria-label="אורך ${p[0]} מטר, רוחב ${p[1]} מטר, גובה ${p[2]} מטר"><div><span>אורך</span><b>${p[0]} מטר</b></div><div><span>רוחב</span><b>${p[1]} מטר</b></div><div><span>גובה</span><b>${p[2]} מטר</b></div></div>`:`<div class="dim missing-dim"><b>${missing}</b></div>`}
const num=v=>v&&/^\d[\d,.]*$/.test(String(v).trim())?v:missing;
const modelOf=g=>[g.room65?.model,g.canopy65?.model,g.canopy75?.model].find(v=>v&&/^[A-Z0-9-]+S$/i.test(v))||missing;
const planName=(url,i)=>{const u=decodeURIComponent(url),view=u.includes("מבט-על")?"מבט על":u.includes("חתך")?"חתך":u.includes("מבט-צד")?"מבט צד":`תוכנית ${i+1}`,use=u.includes("למגורים")?"למגורים":u.includes("למסחר")?"למסחר / תעשייה":"";return `${view}${use?` · ${use}`:""}`};
function metric(n,v,u=""){return `<div class="metric"><span>${n}</span><b>${v}${v!==missing&&u?` ${u}`:""}</b></div>`}
function plans(v,label){if(!v)return `<p>תצורה זו אינה זמינה באתר שמרלינג.</p>`;const p=arr(v.plans).filter(x=>!decodeURIComponent(x).includes("ללא-השתקה"));let html=p.length?p.map((url,i)=>{const a=assets[url]||{};return [a.pdf&&`<a href="${a.pdf}" download>${planName(url,i)} · PDF</a>`,a.image&&`<a href="${a.image}" download>${planName(url,i)} · תמונה</a>`,a.dxf&&`<a href="${a.dxf}" download>${planName(url,i)} · DXF</a>`,a.dwg&&`<a href="${a.dwg}" download>${planName(url,i)} · DWG</a>`,a.storedFrom&&`<span class="warning">קישור ה־PDF הישן לא זמין; נשמרה תצוגת המקור הרשמית</span>`].filter(Boolean).join("")}).join(""):`<span>לא צורפה תוכנית מתאימה באתר שמרלינג</span>`;html+=`<a class="source" href="${v.url}" target="_blank" rel="noopener">מקור רשמי</a>`;return `<div class="plans">${html}</div>`}
function drawingLinks(v){if(!v)return[];const seen=new Set;return arr(v.plans).filter(x=>!decodeURIComponent(x).includes("ללא-השתקה")).flatMap((url,i)=>{const a=assets[url]||{};return [[a.pdf,"PDF"],[a.image,"תמונה"],[a.dxf,"DXF"],[a.dwg,"DWG"]].filter(([path])=>path).map(([path,type])=>({label:`${planName(url,i)} · ${type}`,url:new URL(path,location.href).href}))}).filter(x=>!seen.has(x.url)&&seen.add(x.url))}
function sectionText(g,key){const r=g.room65,a=g.canopy65,b=g.canopy75;const linkText=v=>{const links=drawingLinks(v);return links.length?`\nשרטוטים:\n${links.map(x=>`- ${x.label}: ${x.url}`).join("\n")}`:"\nלא צורפו שרטוטים מתאימים באתר שמרלינג."};if(key==="generator")return `מידות הגנרטור\n${dim(r?.generatorDimensions)}\nמשקל: ${num(r?.generatorWeight)} ק״ג\nקוטר אגזוז: ${num(r?.exhaust)} אינץ׳`;if(key==="room")return `חדר מושתק למגורים\n${dim(r?.roomDimensions)}\nכניסת אוויר: ${num(r?.silencedAirIn)} מ״ר\nיציאת אוויר: ${num(r?.silencedAirOut)} מ״ר${linkText(r)}`;if(key==="canopy65")return `חופה למגורים · 65dB ב־7 מטר\n${dim(a?.dimensions)}\nמשקל כולל: ${num(a?.weight)} ק״ג${linkText(a)}`;return `חופה למסחר / תעשייה · 75dB ב־7 מטר\n${dim(b?.dimensions)}\nמשקל כולל: ${num(b?.weight)} ק״ג${linkText(b)}`}
function summary(g,keys=["generator","room","canopy65","canopy75"]){return `משרד טיקטין חשמל\n${g.kva} KVA\nדגם: ${modelOf(g)}\n\n${keys.map(k=>sectionText(g,k)).join("\n\n")}\n\nמבוסס על נתוני שמרלינג. יש לאמת לפני ביצוע.\nפותח ע״י יובל טיקטין בסיוע AI.`}
const shareChoices=[{key:"generator",label:"מידות הגנרטור"},{key:"room",label:"חדר מושתק למגורים"},{key:"canopy65",label:"חופה למגורים · 65dB"},{key:"canopy75",label:"חופה למסחר / תעשייה · 75dB"}];
function selectedShareKeys(){return [...document.querySelectorAll('.share-options input:checked')].map(x=>x.value)}
function updateShareButtons(){const disabled=!selectedShareKeys().length;document.querySelectorAll('.share-submit').forEach(b=>b.disabled=disabled);const hint=document.querySelector('.share-hint');if(hint)hint.textContent=disabled?"יש לבחור לפחות אפשרות אחת.":"ב־PDF יופיעו רק הנתונים וקישורי השרטוטים שסומנו."}
window.openShare=k=>{document.querySelector('.share-overlay')?.remove();document.body.classList.add('modal-open');const box=document.createElement('div');box.className='share-overlay';box.innerHTML=`<div class="share-dialog" role="dialog" aria-modal="true" aria-labelledby="share-title"><button class="share-close" onclick="closeShare()" aria-label="סגירה">×</button><h3 id="share-title">מה תרצו לשלוח?</h3><p>אפשר לבחור אפשרות אחת או יותר.</p><div class="share-options">${shareChoices.map(x=>`<label><input type="checkbox" value="${x.key}" checked onchange="updateShareButtons()"><span>${x.label}</span></label>`).join("")}</div><p class="share-hint">ב־PDF יופיעו רק הנתונים וקישורי השרטוטים שסומנו.</p><div class="share-steps"><b>1. שמרו את קובץ ה־PDF</b><b>2. פתחו את המייל וצרפו אליו את הקובץ</b></div><div class="share-actions"><button class="share-submit" onclick="pdfSelected(${k})">1 · שמירה כ־PDF</button><button class="share-submit secondary" onclick="emailSelected(${k})">2 · פתיחת מייל</button><button class="cancel" onclick="closeShare()">ביטול</button></div><small>יש לצרף למייל את קובץ ה־PDF שהופק באפליקציה.</small></div>`;box.addEventListener('click',e=>{if(e.target===box)closeShare()});document.body.append(box);box.querySelector('input')?.focus()}
window.closeShare=()=>{document.querySelector('.share-overlay')?.remove();document.body.classList.remove('modal-open')};
window.updateShareButtons=updateShareButtons;
window.emailSelected=k=>{const g=data.find(x=>x.kva===k),keys=selectedShareKeys();if(!keys.length)return;const note=`\n\nלתשומת לב: יש לצרף למייל את קובץ ה־PDF שהופק באפליקציה.`;location.href=`mailto:?subject=${encodeURIComponent(`נתוני גנרטור ${k} KVA`)}&body=${encodeURIComponent(summary(g,keys)+note)}`}
window.pdfSelected=k=>{const g=data.find(x=>x.kva===k),keys=selectedShareKeys();if(!keys.length)return;const text=summary(g,keys).replace(/[&<>]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;"}[c]));const w=open("","_blank");if(!w)return;w.document.write(`<!doctype html><html dir="rtl" lang="he"><head><meta charset="utf-8"><title>נתוני גנרטור ${k} KVA</title><style>body{font-family:Arial,sans-serif;color:#173b39;margin:32px;line-height:1.65}h1{border-bottom:3px solid #ef6a25;padding-bottom:12px}pre{font:inherit;white-space:pre-wrap}small{color:#607572}@media print{button{display:none}}</style></head><body><h1>משרד טיקטין חשמל</h1><pre>${text}</pre><button onclick="print()">הדפסה / שמירה כ־PDF</button></body></html>`);w.document.close();w.focus()}
function show(g,msg=""){const r=g.room65,a=g.canopy65,b=g.canopy75;$("#notice").innerHTML=msg?`<div class="notice">${msg}</div>`:"";$("#result").className="";$("#result").innerHTML=`<button class="back" onclick="back()">← חזור לבחירת גנרטור</button><div class="top"><div><small>הגנרטור המתאים</small><h2>${modelOf(g)}</h2><b>${g.kva.toLocaleString()} KVA Standby</b></div><div class="actions"><button onclick="copyData(${g.kva})">העתקת נתונים</button><button onclick="openShare(${g.kva})">שליחה במייל / PDF</button><button onclick="back()">חזור</button></div></div><div class="grid"><section class="card"><h3>01 · הגנרטור</h3>${dimensions(r?.generatorDimensions)}<div class="metrics">${metric("משקל",num(r?.generatorWeight),"ק״ג")}${metric("קוטר אגזוז",num(r?.exhaust),"אינץ׳")}</div></section><section class="card"><h3>02 · חדר מושתק למגורים</h3>${dimensions(r?.roomDimensions)}<div class="metrics">${metric("כניסת אוויר",num(r?.silencedAirIn),"מ״ר")}${metric("יציאת אוויר",num(r?.silencedAirOut),"מ״ר")}</div>${plans(r,"חדר")}</section><section class="card wide"><h3>03 · חופות אקוסטיות</h3><div class="canopies"><div class="canopy"><b>למגורים · 65dB ב־7 מטר</b>${dimensions(a?.dimensions)}${metric("משקל כולל",num(a?.weight),"ק״ג")}${plans(a,"65")}</div><div class="canopy"><b>מסחר / תעשייה · 75dB ב־7 מטר</b>${dimensions(b?.dimensions)}${metric("משקל כולל",num(b?.weight),"ק״ג")}${plans(b,"75")}</div></div></section></div>`;scrollTo({top:0,behavior:"smooth"})}
function choose(n){if(!n)return;const exact=data.find(x=>x.kva===n),g=exact||data.find(x=>x.kva>=n);if(!g){$("#notice").innerHTML=`<div class="notice">מעל ${data.at(-1).kva} KVA נדרש תכנון מיוחד</div>`;return}show(g,exact?"":`מוצג ההספק הקרוב כלפי מעלה: ${g.kva} KVA`)}
window.back=()=>{$("#notice").innerHTML="";$("#result").className="empty";$("#result").textContent="בחרו הספק כדי להתחיל";$("#kva").value="";$("#models").value=""};
window.copyData=k=>navigator.clipboard.writeText(summary(data.find(x=>x.kva===k)));
Promise.all([fetch("data.json").then(r=>r.json()),fetch("plan-assets.json").then(r=>r.json()).catch(()=>({}))]).then(([d,a])=>{data=d;assets=a;$("#models").innerHTML+=data.map(x=>`<option value="${x.kva}">${x.kva.toLocaleString()} KVA · ${modelOf(x)}</option>`).join("")});
$("#selector").addEventListener("submit",e=>{e.preventDefault();choose(Number($("#kva").value))});$("#models").addEventListener("change",e=>choose(Number(e.target.value)));

